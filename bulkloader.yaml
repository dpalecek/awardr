# Autogenerated bulkloader.yaml file.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: re
- import: base64
- import: google.appengine.ext.db
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:
- kind: StarwoodProperty
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: address
      external_name: address
      # Type: String Stats: 1021 properties of this type in this kind.

    - property: address2
      external_name: address2

    - property: category
      external_name: category
      # Type: Integer Stats: 1021 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: city
      external_name: city
      # Type: String Stats: 1021 properties of this type in this kind.

    - property: coord
      external_name: coord
      import_transform: transform.none_if_empty(db.GeoPt)
      # Type: GeoPt Stats: 925 properties of this type in this kind.

    - property: country
      external_name: country
      # Type: String Stats: 1021 properties of this type in this kind.

    - property: fax
      external_name: fax
      # Type: PhoneNumber Stats: 1021 properties of this type in this kind.
      #import_transform: db.PhoneNumber

    - property: id
      external_name: id
      # Type: Integer Stats: 1021 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: last_checked
      external_name: last_checked
      # Type: Date/Time Stats: 1021 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: name
      external_name: name
      # Type: String Stats: 1021 properties of this type in this kind.

    - property: phone
      external_name: phone
      # Type: PhoneNumber Stats: 1021 properties of this type in this kind.
      #import_transform: db.PhoneNumber

    - property: postal_code
      external_name: postal_code
      # Type: String Stats: 922 properties of this type in this kind.

    - property: state
      external_name: state
      # Type: String Stats: 706 properties of this type in this kind.

